export interface UserSettings {
  phoneNumber: string;
  spotNumber: string;
}

export interface RetrievalData {
  date: string;
  time: string;
}

export interface IconProps {
  className?: string;
  size?: number;
}